# COSMIC 3.2 拆分优化说明

本次以硅基流动 DeepSeek V3.2 为基线，优化精准模式的功能过程提取，覆盖按章节提取、精准一键拆分、覆盖度验证和补充提取。本文记录代码改动与本地验证，线上发布状态以部署平台为准。

## 发现的问题

| 问题 | 代码中的原因 | 处理方式 |
| --- | --- | --- |
| 长文件遗漏后半部分 | 长章节一次提取，阶段一未检查输出截断；旧一键后续轮次仅取文首 16,000 字 | 原文按约 6,000 字分段，带相邻上下文；逐段提取并复核；输出截断时缩小片段重新提取 |
| 简短需求遗漏 | 标题间正文不足 30 字会被忽略、章节不足 50 字默认不选；第一标题前正文丢失 | 保留开头正文和短章节；无可靠模块锚点时保留原文，不用摘录替代全文 |
| 短文无依据扩展 | CRUD 矩阵、指标与维度组合、最低模块数、模块预估等被当作扩展目标 | 精准模式不注入预估数量；每项要求实际触发/操作和业务结果；原文不足时允许 0 或 1 项 |
| 3.2 粒度行为不一致 | 模型类型判断恒为 V4 | 分离实际模型分类，3.2 使用专门的原文约束提示词 |
| 后段功能细节失真 | 每批 ERWX 都只参考文档开头 | 按本批功能原文证据和章节定位上下文 |
| 失败后误以为已完成 | 章节失败会中断后续提取；输出未完成仍可能被解析为有效列表 | 继续其余章节并列出失败章节；精准一键只在提取完整成功后自动拆分 |
| 跨段重复 | 相邻片段同一需求出现“新增/创建”等不同命名 | 仅在证据高度重叠、用户及触发相同且名称同义时合并；独立证据支持的相似功能保留 |

## 新流程

精准模式执行“原文分段 → 提取候选 → 核对遗漏与过拆 → 原文引用校验 → 合并结果 → 按本批原文拆分”。

每项保留 `documentEvidence` 和原文范围。引用必须能在相应原文找到；格式不全、伪造引用和截断输出不能直接计为成功。查看/编辑功能列表时，将鼠标停在功能描述上可查看原文依据。

覆盖度验证也逐片段检查。未通过 JSON 或原文证据校验时明确报错，不把异常当成“没有遗漏”。补充提取仅核验指定遗漏候选，不再把命名澄清自动扩成多个新功能。

## 验证结果

- `npm test`：全部 16 个测试入口通过，包含新模型分类、提取恢复、章节与证据传递、覆盖度及精准 API 处理器测试。
- 真实 HTTP 接口联调：运行实际 Express 服务、后台任务管理器和 AI 客户端，上游为本地模拟模型。6 个场景、24 次请求通过，覆盖短文复核、25,219 字长文的 5 段处理、截断恢复与明确失败、覆盖度验证到补充提取的链路；没有调用外网模型或数据库。
- `npm run build`：构建通过；仍有现有前端大包体积提示。
- 截断与修复用例：覆盖已闭合但 `finish_reason=length` 的输出、复核阶段截断、伪造依据、取消请求、多片段都需要三层恢复、同一证据的同义重复。
- 本地 5 份 Word 文档的分段检查：9,538 / 15,213 / 16,556 / 19,812 / 21,871 字，分别分为 2 / 3 / 3 / 4 / 4 段，所有段拼接后与提取正文完全一致。

以上是离线程序回归、HTTP 联调和原文完整性检查。本地未配置 `SILICONFLOW_API_KEY`，真实 3.2 测试尚未执行，不能据此宣称业务需求覆盖率达到 100%。已增加 `npm run test:cosmic-live`：配置密钥后可运行短文查询、独立操作、无功能需求、长文尾部四组合成样例测试，报告写入 `outputs/`，不会上传本地业务文档。

## 使用与边界

在模型列表选择“DeepSeek-V3.2（硅基流动）”，使用“精准模式”或精准模式下的一键拆分，即可走新流程。运行中的后端需要重启才能加载新代码；本次已生成新的前端构建。

精准提取每段通常需要一次提取和一次复核，请求次数会增加；异常输出才额外恢复。程序核验引用确实存在，业务独立性仍由模型复核和人工审阅共同判断。

用户主动选择的“数量优先”仍保留数量规划逻辑，不等同于精准原文度量。标准/经验增强 ERWX 模板及现有 CFP 计数口径没有在本次重定义；本次主要解决功能过程清单的遗漏和无依据扩展。
